{{ config(materialized='table') }}

SELECT 
    *,
    CASE 
        WHEN pokemon_value_usd > 10000 THEN 'Luxury Pokemon'
        WHEN pokemon_value_usd > 5000 THEN 'Premium Pokemon'
        ELSE 'Standard'
    END AS market_category
FROM {{ ref('int_pokemon_crypto') }}